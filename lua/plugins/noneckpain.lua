return {
  "shortcuts/no-neck-pain.nvim",
  version = "*",        -- install latest stable (optional)
  -- event = "VeryLazy", -- optional: or load on some event
  config = function()
    -- Optional: nothing needed, but you *can* configure if you want
    -- require("no-neck-pain").setup({
    --   width = 100,  -- e.g. center width
    -- })
  end,
}
